name: Update Analytics Dashboard

on:
  schedule:
    # Every 30 minutes
    - cron: '0,30 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-dashboard:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install requests

      - name: Generate summary
        run: python analytics_logger.py

      - name: Commit & push analytics data
        run: |
          git config user.name "BroadFSC Bot"
          git config user.email "bot@broadfsc.com"
          git add analytics/summary.json analytics/posts.json analytics/interactions.json dashboard.html
          git diff --staged --quiet || git commit -m "chore: update analytics dashboard [skip ci]"
          git push origin HEAD:main
